# Database connection URL for PostgreSQL
# This should match the database configuration in docker-compose.yaml
DATABASE_URL=postgresql://postgres:password@localhost:54321/electric

# Create a secret for better-auth
BETTER_AUTH_SECRET=


APP_URL=http://localhost:5174

# Electric (HTTP Shape API)
# Dev: not required — code uses http://localhost:3000 by default
# Prod: REQUIRED — set in your hosting provider, not in the repo
# ELECTRIC_URL=https://electric.example.com
# ELECTRIC_SECRET=replace-with-electric-secret
# ELECTRIC_SOURCE_ID=optional-only-if-required

# Optional
# NODE_ENV=development


# Client envs (only non-secrets; must be prefixed with VITE_)
# Example:
# VITE_SOME_PUBLIC_VALUE=example


# ---------------- Production example (set in hosting env) ----------------
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB
# APP_URL=https://app.example.com
# ELECTRIC_URL=https://electric.example.com
# ELECTRIC_SECRET=your-prod-electric-secret
# ELECTRIC_SOURCE_ID=your-source-id-if-required
# NODE_ENV=production3

